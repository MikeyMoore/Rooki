1. Install CMake: www.cmake.org
2. Download OpenCV source code
3. Create new folder inside opencv folder, name it 'build'
4. Open CMake (it might throw a warning since it'd from an unidentified developer source)
5. On the source code, browse source and set it to the OpenCV folder you just downloaded.
6. On the Where to build binaries, browse build and set it to the build folder you just created.
7. Hit configure, specify Unix Makefiles and click button for Use default native compilers.
8. Click Generate.
9. In terminal, cd to the build folder you created.
10. Type make
11. Type sudo make install

